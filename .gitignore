# Never commit the real token/config
.env

# NOTE: warnings.json / active_trainings.json / help_requests.json are
# intentionally NOT ignored - the GitHub Actions workflow commits them back
# to the repo after each run so bot state survives restarts. See README.

# Python
__pycache__/
*.pyc
.venv/
venv/
